# Griffin Example Data

`tiny.vcf` is synthetic fixture data for development and tests. It is not patient data.

The coordinate is intended for GRCh37-style BRAF V600E smoke testing. Use
`--genome-assembly GRCh37` when running it through real Ensembl VEP REST. Under GRCh38, the
same coordinate resolves to a different locus.
